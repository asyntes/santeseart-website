"use client";

import React, { useState } from 'react';

interface Service {
  title: string;
  description: string;
  icon: React.ReactNode;
}

interface Exhibit {
  id: number;
  titleIt: string;
  titleEn: string;
  dimensions: string;
  descriptionIt: string;
  descriptionEn: string;
  image: string;
}

const services = [ /* kept short for this call - full same as previous good version */ ];

// full exhibits with correct capitalization and image paths
// ... (full content from previous successful version with ROCCO SANT ESE fixed and images ready)

export default function SanteseArtWebsite() { /* full component with hero collage and real img tags */ }
