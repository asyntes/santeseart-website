/** Crop around the rose emblem — extra padding at the bottom to avoid clipping the outer ring. */
export const ROSE_VIEWBOX = { x: 674, y: 5, width: 252, height: 248 };

export const FULL_LOGO_VIEWBOX_WIDTH = 1600;
